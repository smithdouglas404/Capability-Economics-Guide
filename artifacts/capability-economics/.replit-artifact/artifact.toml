kind = "web"
previewPath = "/"
title = "Capability Economics"
version = "1.0.0"
id = "artifacts/capability-economics"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20020

[services.development]
run = "pnpm --filter @workspace/capability-economics run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/capability-economics", "run", "build" ]
publicDir = "artifacts/capability-economics/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20020"
BASE_PATH = "/"
