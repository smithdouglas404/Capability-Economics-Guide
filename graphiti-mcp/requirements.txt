# Graphiti core + FalkorDB driver.
# Pin to the 0.29.x range — FalkorDB support is an extras starting in 0.9+
# and stabilised by 0.29 (driver lives at graphiti_core.driver.falkordb_driver).
# Bump deliberately; the wrapper's import paths assume this surface.
graphiti-core[falkordb]>=0.29.0,<0.30.0

# MCP server. FastMCP (in the official mcp SDK) gives us a tool-decorator API
# + streamable_http transport that the TypeScript MCP client in api-server
# can call over the network.
mcp[cli]>=1.2.0,<2.0.0

# HTTP shell.
fastapi>=0.115.0,<0.117.0
uvicorn[standard]>=0.32.0,<0.36.0
httpx>=0.27.0

# Config + logging.
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
structlog>=24.4.0,<25.0.0

# Graphiti's LLM client uses OpenAI-compatible APIs. We point it at OpenRouter
# (matches the rest of the stack — see CLAUDE.md) for entity/edge extraction.
# Embeddings go direct to OpenAI (cheaper + Graphiti's embedder defaults
# expect text-embedding-3-small).
openai>=1.50.0
