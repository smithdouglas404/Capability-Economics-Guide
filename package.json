{
  "name": "workspace",
  "version": "0.0.0",
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "private": true,
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:deploy": "pnpm run typecheck:libs && pnpm --filter @workspace/inflexcvi run build && pnpm --filter @workspace/api-server run build",
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "start": "pnpm --filter @workspace/scripts run deploy:migrate && pnpm --filter @workspace/api-server run start",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "typecheck:libs": "tsc --build",
    "check:contract": "pnpm --filter @workspace/scripts run check:contract",
    "check:routes": "pnpm --filter @workspace/scripts run check:routes",
    "check:codegen": "pnpm --filter @workspace/scripts run check:codegen"
  }
}
